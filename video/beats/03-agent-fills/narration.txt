This is my own agent, in my own browser. I tell it what happened in one sentence. It calls read claim state, gets the requirements the policy actually asks for, and writes the draft field by field. Watch the badges. Every row it sets is marked agent, so I can see which answers came from a model and which came from me. Nothing here was pre-wired for this insurer. The rules arrived from the page.
