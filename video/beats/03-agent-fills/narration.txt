This is my own agent, in my own browser. I tell it what happened in plain words. It calls read claim state, gets the requirements the policy actually asks for, and writes the draft field by field. Watch the badges. Every row it sets reads via tool, so the page records the route each answer took rather than guessing who typed it. Nothing here was pre-wired for this insurer. The rules arrived from the page.
